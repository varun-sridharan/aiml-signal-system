{
 "version": 1,
 "note": "Content source for the Build Plan page. Edit this; ask Claude to refresh the design from it. status is DONE | NOW | LATER and drives the tile colour, the pill and the sidebar dot. activities[] renders the phase table; notes[] renders the decision log inside the phase; a phase with neither stays a one-line outline row. `lastUpdated` is the date this content last changed. It is provenance for whoever reads this file; the generated pages do NOT render it yet — that lands with the Phase 5 design lock, after which a page behind its source will say so on its face.",
 "site": {
  "kicker": "AI/ML Signal · build plan",
  "title": "Build Plan",
  "lede": "Rolling, session-sized. Each phase is timeboxed to about an hour, with an end state you can check.",
  "headerTitle": "Build Plan",
  "headerNote": "rolling · session-sized",
  "headerLinks": [
   {
    "label": "System Design",
    "href": "../design/System-Design.html"
   },
   {
    "label": "Prompts",
    "href": "../prompts/"
   }
  ],
  "footerNote": "Build Plan · rolling. Detailed phases updated after each session from the decisions we make. Prompts: <a href=\"../prompts/\">prompts/</a> · Design: <a href=\"../design/System-Design.html\">System-Design.html</a> · Backlog: <a href=\"../application/Control-Hub.html\">Control-Hub.html</a>."
 },
 "currentStatus": {
  "date": "2026-09-15",
  "body": "Phases 1–3 complete and the repo is <b>public</b> at <code>github.com/varun-sridharan/aiml-signal-system</code>, with the HTML surfaces served through GitHub Pages. <b>The Framer's faithfulness gate is currently red, and that is the most useful thing in the repo right now.</b> A second golden case built on 2026-09-15 — one with a fabrication planted on purpose — caught the grader failing in both directions: flagging the writer's reasoning as unsourced on a clean day, and missing a real distortion in 3 of 5 runs. Both failures are the grader selecting the wrong sentences to grade, and neither was visible to a suite made only of clean cases. Next: split the Framer's output into <code>facts</code> and <code>interpretation</code> so the grader is only ever shown what is checkable, then re-measure against the baseline in <code>data/evals/baseline-2026-09-15.md</code>. Phase 4 — Scout follows."
 },
 "howItWorks": [
  "<b>Rolling:</b> the next few phases are detailed; later ones are outlined and filled in as we reach them.",
  "<b>1-hour phases:</b> each phase has an end state and fits one working session.",
  "<b>Milestone activities:</b> each says what it accomplishes and why; the <b>how</b> lives in a canonical prompt under <a href=\"../prompts/\">prompts/</a> (final &amp; holistic — see the folder README).",
  "<b>Notes = decision log:</b> Claude maintains the decisions, tradeoffs, and priorities per phase from our sessions, and updates them when a phase concludes.",
  "<b>Content vs design:</b> every document is JSON content rendered by a design from Claude Design. Claude edits the JSON; the HTML is generated and never hand-edited.",
  "<b>Rituals:</b> <em>end of session</em> — Claude updates the concepts reference JSON and reminds you to refresh it. <em>End of phase</em> — Claude reminds you to regenerate <b>all</b> static docs whose JSON changed (concepts, plan, design record), since a phase boundary always moves the plan and the decision log."
 ],
 "phases": [
  {
   "num": 1,
   "id": "phase-1",
   "title": "First commit &amp; the Claude Code loop",
   "status": "DONE",
   "time": "~60 min",
   "endState": "all scaffolding committed &amp; pushed to GitHub via a Claude Code session you drove yourself.",
   "why": "your work becomes versioned and accessible anywhere, and you learn the core approve-edits loop on a low-stakes task before building agents.",
   "activities": [
    {
     "activity": "Review repo with Claude Code",
     "outcome": "You see how it reads the codebase before editing",
     "why": "Builds trust in what it will change",
     "prompt": "<a href=\"../prompts/p1-scaffold-commit.md\">p1-scaffold-commit.md</a>"
    },
    {
     "activity": "Flesh out README",
     "outcome": "README describes the project + real folder structure",
     "why": "Entry point for you and, later, the public",
     "prompt": "<a href=\"../prompts/p1-scaffold-commit.md\">p1-scaffold-commit.md</a>"
    },
    {
     "activity": "First commit + push",
     "outcome": "Everything lands on GitHub",
     "why": "Versioned source of truth",
     "prompt": "<a href=\"../prompts/p1-scaffold-commit.md\">p1-scaffold-commit.md</a>"
    }
   ],
   "notes": [
    "Repo stays private now; when exposed later, exclude <code>Control-Hub.html</code> + <code>data/</code> from the public build (personal telemetry).",
    "Deliberately low-stakes (README + commit) to learn the CLI loop before touching agents.",
    "<b>Done 2026-08-09:</b> committed as <code>e1661d7</code>, pushed to <code>origin/main</code>, generated with Opus 5.",
    "Claude Code added <code>.DS_Store</code> to <code>.gitignore</code> (macOS junk) — one change beyond prompt scope, flagged transparently. Environment hygiene, not a code decision.",
    "Confirmed the pages don't read <code>data/</code> yet (content inline) — wiring deferred to Phases 6 &amp; 9, as designed.",
    "Practice: approved each step individually rather than blanket \"don't ask again\" — the right habit while learning."
   ]
  },
  {
   "num": 2,
   "id": "phase-2",
   "title": "Build &amp; prove the Framer",
   "status": "DONE",
   "time": "~60 min",
   "endState": "a Framer agent that turns <code>data/evals/golden/reference_2026-08-04.json</code> into a brief matching the <a href=\"../application/Signal.html\">Bento reference</a> voice.",
   "why": "Framer quality is the riskiest assumption in the system — if the voice lands, the whole thing is worth building; it's the cheapest bet to de-risk first.",
   "activities": [
    {
     "activity": "Finalize the Framer build prompt (+ Evals)",
     "outcome": "Canonical prompt with faithfulness/usefulness graders baked in",
     "why": "Reproducible recipe; evals from day one",
     "prompt": "<a href=\"../prompts/p2-build-framer.md\">p2-build-framer.md</a>"
    },
    {
     "activity": "Generate the Framer agent via Claude Code",
     "outcome": "<code>agents/framer.py</code> runs on the sample day",
     "why": "Your first real agent",
     "prompt": "<a href=\"../prompts/framer-prompt.md\">framer-prompt.md</a>"
    },
    {
     "activity": "Eyeball output vs the reference",
     "outcome": "You judge whether the voice sounds right",
     "why": "The go / no-go on the whole system",
     "prompt": "<span class=\"pending\">— your taste is the gate</span>"
    }
   ],
   "notes": [
    "Locked Framer rules: worked examples on <b>AI items only</b>; synthesis \"thread\" required; <b>DO THIS</b> reserved for AI; reading-time header; inline connections.",
    "Faithfulness = hard zero-hallucination gate (objective); usefulness = soft, learned target (subjective).",
    "Test against <code>application/Signal.html</code>, not vibes.",
    "<b>Done 2026-08-09 (commit df8adfd, Opus 5):</b> Framer built &amp; run — 7 items, 2 DO THIS, ~5 min read, <b>$0.10</b>. Batched to 2 calls (Opus framing + Haiku check) with prompt caching + cost breaker, all working.",
    "<b>Voice verdict = GO:</b> thread carries a real correlation + contradiction and reaches its own conclusion; connections genuine. Two weak spots to tune later: iStar example clause doesn't parse; Kimi K3 example vaguer than the reference's concrete \"~1/10th cost\".",
    "<b>Key finding → Phase 3:</b> the faithfulness check produced a <b>false positive</b> (flagged $965B as unsupported though it was in the excerpt). Two grader bugs: it missed a clause, and it graded thread/headline text against a single item's excerpt. The grader needs its own eval; the thread must be checked against the <em>union</em> of excerpts.",
    "Deviations, both fine: built the prompt file fresh (option 3, no invented Scout list); switched the framing call to streaming after the SDK blocked a 24k-token non-streaming request (no spend)."
   ]
  },
  {
   "num": 3,
   "id": "phase-3",
   "title": "Framer eval harness (golden set)",
   "status": "DONE · DEFECT REOPENED",
   "time": "~60 min",
   "endState": "a tiny offline eval that scores the Framer (faithfulness assertion + usefulness rubric) against 1–2 labeled days.",
   "why": "turns \"is the voice right?\" from a vibe into a number, and makes every future prompt tweak safe via regression.",
   "activities": [
    {
     "activity": "Build a golden set from labeled days",
     "outcome": "1 day frozen in <code>data/evals/golden/2026-08-04/</code> (input + output + labels)",
     "why": "The fixed yardstick for regression",
     "prompt": "<a href=\"../prompts/p3-framer-evals.md\">p3-framer-evals.md</a>"
    },
    {
     "activity": "Faithfulness grader (claim decomposition)",
     "outcome": "Grader rewritten: claim ledger with quoted spans, correct scope, code-verified evidence",
     "why": "Objective hard gate; catches hallucination",
     "prompt": "<a href=\"../prompts/p3-framer-evals.md\">same</a>"
    },
    {
     "activity": "Usefulness rubric + run script",
     "outcome": "<code>data/evals/run_evals.py</code> — one command, exit code gates",
     "why": "Safe iteration on the Framer prompt",
     "prompt": "<a href=\"../prompts/p3-framer-evals.md\">same</a>"
    }
   ],
   "notes": [
    "Eval harness lives in the repo from early on; the Tuner-driven learning matures later (needs weeks of data).",
    "Held-out slice kept separate from anything the Tuner adapts on (train/test hygiene).",
    "<b>First golden-set case comes free:</b> the Phase 2 grader false positive. Fix the check to (a) not miss clauses and (b) grade the thread against the union of all item excerpts, not one item's.",
    "<b>Done 2026-08-19 (Opus 5):</b> <code>run_evals.py</code> runs three checks — faithfulness (hard gate, 1 Haiku call), structural assertions (hard gate, free), usefulness rubric (soft, reported). Both seeded regressions pass; run costs <b>~$0.02</b>.",
    "<b>The grader's real bug was scope, not carelessness.</b> It was shown the excerpt first and the writing second, so it enumerated the <em>excerpt's</em> claims and never checked the framing at all — which is why $965B \"failed\" and why fabricated facts sailed through. Writing now comes first in the payload, the excerpt is labelled ground truth, and each claim must carry a verbatim span.",
    "<b>Second false positive found while labelling:</b> the 2026-08-13 re-run flagged \"~80% of Q1 dollars\", which the excerpt states word for word. Seeded as a second regression case.",
    "<b>Two extras beyond the brief,</b> both earned by a failing test: a code check that every quoted span is literally in the excerpt (a grader can invent a citation; a substring match can't), and <code>temperature=0</code> on the grader — at default sampling it disagreed with itself on ~1 run in 3, which would have failed builds at random. 5/5 clean runs after.",
    "<b>Known limit, carried to Phase 8:</b> the gate is deterministic per input, but on <em>unseen</em> days the grader still sometimes enumerates an inference as a claim. Fine for regression (the golden day is fixed); revisit before the grader guards live briefs — likely by moving it to a stronger model.",
    "Usefulness stays ungated until calibrated against your own ratings. Judge runs on Haiku to keep spend low; upgrade it the day it starts gating.",
    "<b>Reopened 2026-09-15.</b> A second golden case, <code>golden/negative-01/</code>, was added to test the direction the suite could not test: every unit until now expected <code>expected_unsupported: []</code>, so a grader that flagged nothing passed everything. On its first run the new case failed, and re-running both cases five times each measured the grader rather than guessing at it. <b>2026-08-04:</b> 18 false positives, identical in all five runs — every one an implication, recommendation or worked-example scenario, which the grader's own prompt names as never-enumerate. <b>negative-01:</b> the planted distortion (“raised $965B” where the source says “valued at $965B”) was caught in 2 of 5 runs; in the other 3 the grader enumerated the <em>source's</em> sentences instead of the writing's — the same scope bug this phase closed, returning on the first case it had not been tested against. Ruled out: model drift (one snapshot, the alias never moved), sampling noise (byte-identical 5/5 on the clean case), mis-wired inputs (verified: <code>framing_from_framed_day(case[“output”])</code>), and wrong labels (they match the prompt's own rules). The harness itself is sound and did its job — the grader it grades is not. Numbers in <code>data/evals/baseline-2026-09-15.md</code>. Status left at DONE because the deliverable of this phase is the harness, and the harness is what found this."
   ]
  },
  {
   "num": 4,
   "id": "phase-4",
   "title": "Scout — seed sources + fetch",
   "status": "NOW",
   "time": "~60 min",
   "endState": "Scout pulls candidate items from the fixed seed list into <code>data/</code>. <b>Opens with an extraction, before any Scout code.</b> The cost breaker and the stop-reason guard live inside <code>framer.py</code>; Scout is their <em>second</em> caller, which is the moment to extract rather than copy. Two new modules: <code>agents/budget.py</code> (pricing table, usage ledger, <code>estimate_cost_usd</code>, <code>month_to_date_spend</code>, <code>record_call</code>, plus new <code>reserve_for(calls)</code> / <code>can_afford()</code>) and <code>agents/llm.py</code> (<code>structured_text</code> stop-reason guard, client factory). <b>Upgrade the breaker while moving it:</b> today's check is <code>spent >= budget</code>, a stop-<em>after</em> that can overshoot by one run. Replace with reserve-then-gate — <code>spent + reserve_for(planned_calls) > budget</code> refuses to start, where the reserve is the worst case at each call's <code>max_tokens</code> cap. <code>reserve_for</code> takes a list of planned calls, not a constant, because Scout's shape is many small calls where the Framer's is one large one. <b>Also add <code>agents/__init__.py</code></b> so <code>agents/</code> is a real package and the <code>sys.path.insert</code> hack in <code>run_evals.py</code> goes away before three modules import each other.",
   "why": "turns on real intake so the Framer runs on live items, not a sample. The extraction comes first because a guard that protects money must exist in exactly one place — four copies is four chances to get it wrong, and the fourth is found by a bill.",
   "activities": [],
   "notes": []
  },
  {
   "num": 5,
   "id": "phase-5",
   "title": "Gatekeeper basics + Suppressed drawer · <b>design lock</b>",
   "status": "LATER",
   "time": "~60 min",
   "endState": "provenance + corroboration + dedup filtering, with everything killed logged to the Suppressed drawer. <b>Also:</b> a volume guard — the Gatekeeper caps how many items reach the Framer, and the Framer asserts loudly on item count rather than silently truncating. <b>And the design lock:</b> Signal.html and Control-Hub.html are <em>live surfaces</em> whose content changes daily, so their design must be finalised here — Claude Design delivers a <b>renderer with a data slot</b>, not a regenerated page, and after this point the visuals stop changing while the agents feed it. Templates must also render <code>lastUpdated</code> from the content JSON, so a page that is behind its source says so on its face instead of relying on memory.",
   "why": "kills noise and gives you the false-negative audit trail that keeps FOMO dead. The volume guard protects the Framer's single-call design — a 60-item day means the Gatekeeper failed, and should fail loudly rather than drop items. The design lock has to land <em>before</em> Phase 6 wiring: you can't regenerate a page in a design tool every morning, so the live surfaces need a fixed template the pipeline writes into.",
   "activities": [],
   "notes": []
  },
  {
   "num": 6,
   "id": "phase-6",
   "title": "Daily loop end-to-end (News renders from data/)",
   "status": "LATER",
   "time": "~60 min",
   "endState": "a real daily brief generated Scout→Gatekeeper→Framer→News.html, with quiet-day behavior. <b>Plus three closures found during the Phase 3 walkthrough:</b> (1) <b>retry loop</b> — the Framer regenerates a flagged item and re-checks before writing, instead of flagging and shipping anyway; (2) <b>daily smoke test</b> — run the free structural checks on each day's output so contract violations surface immediately, not at the next prompt edit; (3) <b>eval regenerate mode</b> — optionally re-run the Framer on the golden input so a <em>Framer</em> prompt change is regression-tested, not just the grader.",
   "why": "the anti-FOMO daily value goes live — you start using it every morning. The three closures turn \"flagged\" into \"fixed\", catch drift the day it happens, and close the gap that today's harness only regression-tests the grader, never the writer.",
   "activities": [],
   "notes": [
    "<b>Negative case landed early (2026-09-15).</b> <code>golden/negative-01/</code> was built ahead of this phase because the missed-hallucination path had no automatic coverage at all. The remaining eval closures stay here: a regenerate mode so a change to the Framer's prompt is exercised, a bounded retry on flagged claims, and a daily smoke test for the free structural checks."
   ]
  },
  {
   "num": 7,
   "id": "phase-7",
   "title": "Coach — standalone homework",
   "status": "LATER",
   "time": "~60 min",
   "endState": "a weekly 60-min exercise generated from the week's items, written to the backlog. <b>Also: capture the skip reason</b>, not just the skip. \"Wrong topic\" and \"too big\" correct different agents — the Framer's priors versus the Coach's scoping — and an undifferentiated skip is uninterpretable by either.",
   "why": "turns reading into doing (job b), before the harder repo-grounding step.",
   "activities": [],
   "notes": []
  },
  {
   "num": 8,
   "id": "phase-8",
   "title": "Coach — repo grounding",
   "status": "LATER",
   "time": "~60 min",
   "endState": "homework that fits a real spot in your repos, verified by assertions against the code. <b>Also: prior-art reconciliation.</b> The same repo access that grounds an exercise answers whether I have already built the thing — states are <em>none / partial / already built</em>, and an exercise found to be already built is retired with a pointer to the code instead of being offered. Recall-biased: when unsure, offer it with a note rather than suppress it, because a wrong suppression is invisible and a wrong offer costs minutes. Built on the <b>Claude Agent SDK</b> — the first and, by design, the only agent here that needs a real agent loop.",
   "why": "the magic feature — and the highest hallucination risk, so it comes after trust is built.",
   "activities": [
    {
     "activity": "Build the Coach on the Claude Agent SDK",
     "outcome": "<code>agents/coach.py</code> runs on the SDK's agent loop with Read, Grep and Glob scoped to the declared repo paths",
     "why": "First agent here that genuinely needs tool use — it must search a codebase it has not seen",
     "prompt": "—"
    },
    {
     "activity": "Prior-art reconciliation as a subagent",
     "outcome": "A focused subagent answers <em>none / partial / already built</em> per exercise, with file and line as evidence",
     "why": "Recall-biased search is a different question from grounding, and keeping it separate keeps its context clean",
     "prompt": "—"
    },
    {
     "activity": "Spend + scope enforcement via SDK hooks",
     "outcome": "<code>agents/budget.py</code> wired as a pre-tool hook; path allowlist enforced as a permission rule",
     "why": "Retires <code>concept-hooks</code> from PLANNED, and keeps the reserve check in force where the loop has no natural bound",
     "prompt": "—"
    }
   ],
   "notes": [
    "<b>Why the SDK here and nowhere else (decided 2026-09-15).</b> Every agent before this one is a <em>pipeline</em>: fixed steps, no tools, a closed world where everything arrives in the prompt. The Framer is two API calls in a known order — giving it a planning loop would hand the model the control flow, which is the opposite of what this system argues for. The Coach is the first agent whose steps genuinely cannot be written in advance, because it has to search a repository it has not seen. That is what the SDK is for, and building the loop, the tool dispatch and the context management by hand would be re-implementing it worse.",
    "<b>The bound problem, and how it is handled.</b> The cost breaker gates on a <em>certain</em> worst case — input tokens plus <code>max_tokens</code> at the output price. An agent loop has no such bound: the number of turns is decided at run time. So the Coach gets a second limit the other agents do not need — a hard cap on tool-call turns, plus the reserve check re-run as a pre-tool hook before each turn rather than once before the run. A run that hits either ceiling stops and reports, and never writes a partial exercise.",
    "<b>What stays outside the loop.</b> Grounding assertions are ordinary Python, not SDK tools. Whether a claimed file path exists, whether a quoted symbol appears in it, whether the exercise names a real function — all of that is checkable without a model, so it is checked without one. The same split as the Framer: the model finds candidates, code confirms them.",
    "<b>Prior-art reconciliation is the same access, the opposite question.</b> Grounding asks <em>where would this fit</em>; reconciliation asks <em>have I already built it</em>. Recall-biased on purpose: a wrong suppression is invisible and a wrong offer costs minutes. States are none / partial / already built, and <b>built is terminal</b> — the exercise retires with a pointer to the code rather than carrying a status.",
    "<b>Not Managed Agents.</b> Considered and refused. That product is for long-running asynchronous work you do not want to host; this is a batch job that runs once a day. It would also move the spend ledger and the budget breaker outside this repo, and those are two of the things the repo is about."
   ]
  },
  {
   "num": 9,
   "id": "phase-9",
   "title": "Control Room live (metrics.json)",
   "status": "LATER",
   "time": "~60 min",
   "endState": "the Control Room renders real eval metrics, pending actions, and elicitation questions — plus the front-door <b>Orchestrator</b> goes live (routes user actions: add-source → Scout, question → chat, feedback/intent → Tuner; the one live-API agent).",
   "why": "closes the report→elicit→label loop so the system can improve deliberately.",
   "activities": [],
   "notes": []
  },
  {
   "num": 10,
   "id": "phase-10",
   "title": "Tuner — learned personalization",
   "status": "LATER",
   "time": "multi-session",
   "endState": "source reputation + topic priors that adapt from your feedback, with the diversity guardrail. <b>Guardrail:</b> completion rate is a diagnostic, never an optimisation target. Optimising it teaches the Coach to generate trivial exercises, which raises the metric while degrading the product. Read it only alongside actual-vs-estimated time and whether the code was reused.",
   "why": "the system starts getting better at <em>you</em> — but only after weeks of data exist.",
   "activities": [],
   "notes": []
  },
  {
   "num": 11,
   "id": "phase-11",
   "title": "Commercial prep (documented)",
   "status": "LATER",
   "time": "multi-session",
   "endState": "shared-base / per-user split proven; auth + multi-tenant scoped when/if productizing.",
   "why": "the path from personal system to product, kept open by the <code>user_id</code> seam.",
   "activities": [],
   "notes": []
  }
 ],
 "crossCutting": {
  "title": "Cross-cutting infrastructure",
  "note": "A trajectory, not a single phase.",
  "items": [
   "<b>Data layer:</b> JSON files now → <b>SQLite</b> when history/queries get awkward (~Phase 6–9) → <b>Postgres + pgvector</b> incl. auth <code>sessions</code> at multi-tenant (Phase 11). The <code>user_id</code> seam keeps the migration mechanical.",
   "<b>Observability:</b> extend <code>usage.json</code> into a structured per-run trace now → an LLM-observability tool at deploy (~Phase 6+).",
   "<b>Cost tracker:</b> ledger + circuit-breaker exist now → Control Room charts it live in Phase 9."
  ]
 },
 "lastUpdated": "2026-09-15",
 "nav": {
  "current": "Plan &amp; Progress",
  "items": [
   {
    "label": "Product",
    "href": "../application/Product.html"
   },
   {
    "label": "Signal",
    "href": "../application/Signal.html"
   },
   {
    "label": "Practice",
    "href": "../application/Practice.html"
   },
   {
    "label": "Control Hub",
    "href": "../application/Control-Hub.html"
   },
   {
    "label": "System Design",
    "href": "../design/System-Design.html"
   },
   {
    "label": "Plan &amp; Progress",
    "href": "../plan/Plan.html"
   }
  ]
 }
}